{
    "name": "Basic Dev Container",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    "mounts": ["source=/var/mnt/Disk2/projects/dcutil/test_docker_native,target=/workspaces/test_docker_native,type=bind,consistency=cached"],
    "workspaceFolder": "/workspaces/test_docker_native",
    "remoteUser": "vscode",
    "containerUser": "vscode",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.vscode-json",
                "ms-vscode.vscode-git"
            ]
        }
    },
    "postCreateCommand": "bash -lc 'set -eux; if command -v sudo >/dev/null; then sudo apt-get update && sudo apt-get install -y --no-install-recommends curl git; else apt-get update && apt-get install -y --no-install-recommends curl git; fi; mkdir -p /workspaces/test_docker_native; if id -u vscode >/dev/null 2>&1; then if command -v sudo >/dev/null; then sudo chown -R vscode:vscode /workspaces/test_docker_native || true; else chown -R vscode:vscode /workspaces/test_docker_native || true; fi; else echo 'User vscode not found in image, skipping chown'; fi'"
}
